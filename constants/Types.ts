export default interface nextSevenDaysType {
  day: string;
  formmatedDate: string;
  id: string | number[];
}

export default interface timeListType {
    time: string; 
    id: string | number[]
  }

  export default interface weightRangeType {
    weightRange: number[]
  }